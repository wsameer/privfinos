# Root environment variables (if any shared across services)
# Copy this to .env and fill in your values

# PostgreSQL
POSTGRES_DB=privfinos
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme

# Optional: Set these if you want to override app-specific settings
# API_PORT=3001
# WEB_PORT=5173
