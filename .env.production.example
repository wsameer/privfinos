# Production Environment Variables
# Copy this file to .env.production and fill in your actual values

# Application Environment
NODE_ENV=production

# Server Configuration
PORT=3001

# Logging
LOG_LEVEL=info

# Database Configuration (PostgreSQL)
# Uncomment and configure when you're ready to use the database
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=privfinos

# CORS Configuration
# In production with Docker, the web app is served from the same origin
# so this can be set to the app's URL
CORS_ORIGIN=http://localhost:3001

# Optional: pgAdmin Configuration (uncomment if using)
# PGADMIN_EMAIL=admin@privfinos.local
# PGADMIN_PASSWORD=admin_password_here

# Optional: Add your own environment variables below
# JWT_SECRET=your_jwt_secret_here
# API_KEY=your_api_key_here
